# GLM-4.6V-Flash 图像理解 MCP 服务器依赖
# 注意：mcp 2.x 已移除 mcp.server.fastmcp（FastMCP 拆分为独立包），
# 因此锁定 1.x 以保证 from mcp.server.fastmcp import FastMCP 可用。
mcp>=1.9,<2.0
httpx>=0.27.0
python-dotenv>=1.0.0
Pillow>=10.0.0
